#!/bin/sh

# ctb - create temporary backup(s)

if [ ! "$1" ]; then
    echo supply a file or directory
fi
bakname="bak__$1$(date +__%Y-%m-%d-%s)"
tar -zcvf "${bakname}.tar.gz" "$1" && echo Saved as "${bakname}.tar.gz"
