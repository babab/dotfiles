[core]
	excludesfile = ~/.gitignore_global

;### identity and signing ###;
; User configuration section is saved unversioned in .gitconfig.user
; with these fields:
;
; [user]
; 	name = Tonnie Diederniettoedoet
; 	email = tonnie@example.com
; 	signingkey = ~/.ssh/sshsignkey.pub
[include]
	path = ~/.gitconfig.user
[gpg]
	format = ssh
[gpg "ssh"]
	allowedSignersFile = ~/.ssh/allowed_signers_git

;### workflow settings ###;
[alias]
	all = !tig --all
	br = branch
	ca = commit --amend --reset-author
	ci = commit
	co = checkout
	di = diff
	fa = fetch --all --verbose
	gh = remote-gh
	log1 = log --oneline --no-show-signature
	ls = log --stat
	mrg = merge --ff-only
	sl = !git branch -a && git stash list
	st = status
[color]
	branch = auto
	diff = auto
	interactive = auto
	status = auto
[pull]
	ff = only
[push]
	default = simple
[init]
	defaultBranch = main
[commit]
	gpgsign = true
[tag]
	gpgsign = true
[log]
	showSignature = true

; vim: set noet ts=4 sw=4 sts=3 ai:
